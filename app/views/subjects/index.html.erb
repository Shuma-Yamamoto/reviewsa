<div class="mt-3">
  <h2>参考書を探す</h2>
</div>

<div class="mt-4">
  <% @subjects.each do |sub| %>
    <div class="card border-dark mb-3" style="max-width: 10rem;">
      <%= link_to university_students_sign_up_path, class: "text-decoration-none text-dark" do %>
        <div align="center"><%= sub.name %></div>
      <% end %>
    </div>
  <% end %>
</div>
<%= render 'layouts/footer' %>