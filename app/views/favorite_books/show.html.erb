<div class="mt-3">
  <h2>マイリスト</h2>
</div>

<h4 class="mt-4">お気に入りの参考書</h4>

<% if @favorite.present? %>
  <% @favorite.each do |f| %>
    <div class="card border-dark mb-3" style="width: 100%;">
      <%= render 'layouts/book', link: book_path(id:f.book_id), b: f.book %>
    </div>
  <% end %>

<% else %>
  お気に入りの参考書はありません。
<% end %>

<div class="mt-4">
  <%= paginate @favorite %>
</div>

<div class="mt-3">
  <%= link_to 'お気に入りのレビューを見る', favorite_reviews_list_path(id:@examinee.id) %>
</div>
